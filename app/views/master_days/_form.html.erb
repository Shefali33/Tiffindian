<%= form_with(model: master_day, local: true) do |form| %>
  <% if master_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_day.errors.count, "error") %> prohibited this master_day from being saved:</h2>

      <ul>
      <% master_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
