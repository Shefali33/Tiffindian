<%= form_with(model: master_subscription, local: true) do |form| %>
  <% if master_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_subscription.errors.count, "error") %> prohibited this master_subscription from being saved:</h2>

      <ul>
      <% master_subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
